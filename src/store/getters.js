export const playlist = state => { return state.playlist }

export const currentSong = state => { return state.currentSong }

export const currentIndex = state => { return state.currentIndex }

export const currentPlayFlag = state => { return state.currentPlayFlag }